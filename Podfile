# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Realm DB Auth' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
    pod 'RealmSwift'

  # Pods for Realm DB Auth

  target 'Realm DB AuthTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Realm DB AuthUITests' do
    # Pods for testing
  end

end
